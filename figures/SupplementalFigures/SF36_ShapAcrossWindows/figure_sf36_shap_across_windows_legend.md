**SF36 | Total |SHAP| Across the AUG and Stop-Codon Windows.** Per-position |SHAP| summed across the nine input channels of the deep-learning model, comparing NMD-susceptible (orange) and Control (blue) transcripts on the held-out test cohort (NMD n = 2,268, Control n = 7,863). Each transcript's ATG and stop windows are centered on the priority ORF's start and stop codons — priority is reference-CDS ORF (ref-AUG traced from the gene's dominant non-NMD isoform), then TransDecoder2 CDS ORF, then highest-Kozak-scoring ORF (see Methods, Priority ORF Selection). Values are smoothed with an 11-nt rolling mean over the raw per-position |SHAP| to abstract over trinucleotide-frame periodicity. **A)** AUG window covering positions -250 to +249 relative to the priority ORF's start codon. **B)** Stop codon window covering the same relative range around the priority ORF's stop codon. Pooled across 5 DeepSHAP runs (500 background samples each) on the atg500_stop500 model configuration.
